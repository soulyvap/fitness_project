import 'package:flutter/material.dart';

class MembersModalContent extends StatelessWidget {
  const MembersModalContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
